<h1>Songs</h1>

<table>
    <tbody>
        <% @songs.each do |song| %>
            <tr>
                <th>
                    <%= link_to song.title, song_path(song)%>
                </th>
            </tr>
        <% end %>
    </tbody>
</table>